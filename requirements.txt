streamlit>=1.40.0
streamlit-pdf-viewer>=0.0.28

# PDF parsing
pymupdf>=1.24.0           # `fitz` — used directly by app.py, parsers.py, highlighter.py
pymupdf4llm>=0.0.17
# docling — optional, slow parser backend with heavy ML deps
# (PyTorch + ONNX runtime, ~5 GB).  Install separately if you need it:
#     pip install "docling>=2.15.0"
# The "docling" parser option in the UI is auto-hidden when not installed.

# Lookup sources
arxiv>=2.1.3
requests>=2.32.0
rapidfuzz>=3.0.0
scholarly>=1.7

# LLM repair (both backends share these)
anthropic>=0.94.1         # LLM_BACKEND=anthropic
openai>=1.40.0            # LLM_BACKEND=aqueduct (OpenAI-compatible API)
pydantic>=2.0             # structured output schemas

# Misc
pandas>=2.2.0
python-dotenv>=1.0.0
